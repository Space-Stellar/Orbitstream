[package]
name = "stream"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.0"

[dev_dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
